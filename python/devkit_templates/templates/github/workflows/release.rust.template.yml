# Installed and kept current by `devkit setup-project`; edits are replaced on the next run.
# Triggered by `devkit release`, which creates the GitHub release with no assets and then
# waits for this workflow to build, attach and publish them. Wheels are built per platform
# with maturin and the `devkit-container` entrypoint binary alongside them (static musl on
# Linux); every artefact is attached and published from one job, so a partial publish can
# only follow a complete build.
name: Release

on:
  release:
    types: [published]

concurrency:
  group: release-${{ github.event.release.tag_name }}
  cancel-in-progress: false

# Read-only by default; only the publish job may write (attach assets to the release).
# The build jobs run project and dependency code, which must not hold a write token.
# Every checkout uses `github.sha`, the commit the release's tag pointed at when the
# event fired: the tag itself could be moved while a job is queued.
permissions:
  contents: read

env:
  TAG: ${{ github.event.release.tag_name }}

jobs:
  build:
    name: Wheel + container (${{ matrix.target }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: true
      matrix:
        include:
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            container_target: x86_64-pc-windows-msvc
            exe: .exe
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            manylinux: "2_17"
            # The container binary is fully static so the image needs no runtime libs.
            container_target: x86_64-unknown-linux-musl
            exe: ""
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          persist-credentials: false

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }},${{ matrix.container_target }}

      - uses: astral-sh/setup-uv@v5

      - name: The tag must name the committed version
        shell: bash
        run: |
          v="$(uv version --short)"
          [ "v$v" = "$TAG" ] || { echo "tag $TAG does not match pyproject.toml version $v" >&2; exit 1; }

      - uses: PyO3/maturin-action@v1
        with:
          target: ${{ matrix.target }}
          # Empty on Windows, where the input is ignored.
          manylinux: ${{ matrix.manylinux }}
          args: --release --strip --out dist

      # The container entrypoint, named by target so the Dockerfile's ADD URL is stable.
      - name: Build the container entrypoint binary
        shell: bash
        run: |
          cargo build --release -p aeth-devkit-container --target "${{ matrix.container_target }}"
          mkdir -p dist
          cp "target/${{ matrix.container_target }}/release/devkit-container${{ matrix.exe }}" \
             "dist/devkit-container-${{ matrix.container_target }}${{ matrix.exe }}"

      - uses: actions/upload-artifact@v4
        with:
          name: wheel-${{ matrix.target }}
          path: dist/*.whl
          if-no-files-found: error

      - uses: actions/upload-artifact@v4
        with:
          name: container-${{ matrix.target }}
          path: dist/devkit-container-*
          if-no-files-found: error

  sdist:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          persist-credentials: false

      - uses: dtolnay/rust-toolchain@stable

      - uses: astral-sh/setup-uv@v5

      - name: Build the sdist
        run: uv build --sdist --out-dir dist

      - uses: actions/upload-artifact@v4
        with:
          name: sdist
          path: dist/*.tar.gz
          if-no-files-found: error

  publish:
    needs: [build, sdist]
    runs-on: ubuntu-latest
    permissions:
      contents: write
      # setup-project: if-no-publish-index
      id-token: write
      # setup-project: end
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          persist-credentials: false

      - uses: astral-sh/setup-uv@v5

      - uses: actions/download-artifact@v4
        with:
          path: dist
          merge-multiple: true

      - name: The tag must name the committed version
        shell: bash
        run: |
          v="$(uv version --short)"
          [ "v$v" = "$TAG" ] || { echo "tag $TAG does not match pyproject.toml version $v" >&2; exit 1; }
          ls -l dist

      # The tag is mutable: the release for it can be deleted and recreated while the
      # build ran. `github.event.release.id` is the release this run was triggered by; a
      # different one now owning the tag must not receive these artefacts.
      - name: The release must still be the one that triggered this run
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_ID: ${{ github.event.release.id }}
        run: |
          current="$(gh release view "$TAG" --json databaseId --jq .databaseId)"
          [ "$current" = "$RELEASE_ID" ] || { echo "release $TAG is now #$current, not #$RELEASE_ID which triggered this run; not publishing" >&2; exit 1; }

      - name: Attach artefacts to the release
        env:
          GH_TOKEN: ${{ github.token }}
        run: gh release upload "$TAG" dist/* --clobber

      # setup-project: if-publish-index
      - name: Publish to {publish_index}
        env:
          UV_PUBLISH_USERNAME: ${{ secrets.UV_INDEX_{publish_index_key}_USERNAME }}
          UV_PUBLISH_PASSWORD: ${{ secrets.UV_INDEX_{publish_index_key}_PASSWORD }}
        run: uv publish --index {publish_index} dist/*.whl dist/*.tar.gz
      # setup-project: end
      # setup-project: if-no-publish-index
      - name: Publish to PyPI
        run: uv publish --trusted-publishing always dist/*.whl dist/*.tar.gz
      # setup-project: end
